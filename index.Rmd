---
title: "Notes personnelles : procédures et outils avec R"
author: "Nicolas Bressoud"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output: bookdown::gitbook
site: bookdown::bookdown_site
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Préambule

Raisons de ce livre. Moyens de garder au chaud mes apprentissages, les principes importants et procédures R relatives. Les soucis, les bonnes pratiques. Les sources aidantes.
<!--
When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
-->
